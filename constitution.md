1. 核心研发原则
先规范后代码：所有功能开发前，必须在 specs/ 目录下存在对应的业务逻辑描述。

防御性编程：爬虫与自动化脚本必须预见“目标变化”，对 UI 变更、网络波动、反爬升级具备容错处理。

代码整洁度：遵循 PEP8 规范。变量名应具有语义化（英文），但注释必须使用中文以便团队评审。

2. 技术栈约束 (Tech Stack)
语言：Python 3.10+

Web 采集：优先使用 Playwright（异步模式），严禁使用未经封装的 requests 进行大规模抓取。

手机自动化：使用 uiautomator2。

日志系统：统一使用 loguru，日志必须包含：设备 ID、任务 ID、耗时、异常堆栈。

3. 安全与合规（领导最看重的部分）
频率控制：单设备/单账号抓取频率严禁超过业务基准值（默认 1次/3秒），必须内置 random.uniform 抖动。

脱敏要求：采集到的用户信息（手机号、ID）在进入数据库前必须进行 MD5 或遮蔽处理。

资源回收：脚本结束或报错退出时，必须强制执行，严禁产生僵尸进程。

4. 交付质量标准 (Definition of Done)
自愈能力：UI 定位失败时，脚本需自动重试 3 次，并在第 3 次失败时保存当前屏幕截图至 debug/screenshots。

可观测性：必须生成简易的实时进度条（如 tqdm）。